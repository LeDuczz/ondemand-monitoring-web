# On-demand Monitoring Web

React + TypeScript web application scaffold for the On-demand Monitoring
platform.

## Commands

```bash
npm install
npm run dev
npm run lint
npm run test
npm run build
```

Business features are intentionally not implemented yet. Feature boundaries
are prepared under `src/features`.
